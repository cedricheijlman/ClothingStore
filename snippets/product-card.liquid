<section class="flex flex-col items-center justify-center">
  <a href={{ productUrl }}>
    <img class="w-[250px] mb-6 mx-4" src="{{ productImg | img_url: '600x600'}}"/>
  </a>
  <h2 class="uppercase text-[14px] font-bold">{{ productName }}</h2>
  <span class="text-[14px]">{{ price | money }}</span>

</section>